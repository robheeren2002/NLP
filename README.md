# NLP
To run the application, first update the API keys in keys/API_keys.py. For now, this key has already been filled, but this might change in the future. 

Afterwards,  run the command:
streamlit run app.py  

Make sure all dependencies are installed.
